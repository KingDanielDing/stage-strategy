services:
  - type: web
    name: stage-strategy-smoke
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn smoke_test:app -b 0.0.0.0:$PORT --timeout 120"
